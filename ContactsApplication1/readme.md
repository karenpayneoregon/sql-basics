# About

- Used to validate database schema and data access code.
- `Create database` script under folder `DatabaseScripts` and name it `CreateDatabase.sql`.
- Database connection string  in `appsettings.json` file.